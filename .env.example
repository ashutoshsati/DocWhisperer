# Copy this file to .env and fill in real values. .env should NEVER be committed.

# OpenAI API key — used for embeddings (ingest.py) and chat completions (query.py).
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...
